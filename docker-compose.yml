# temp feature test
#
# ── Dev backend test page ────────────────────────────────────────────────────────────
#   http://localhost:8000/unicart <- UniCart HTML dev test page
#
# ── API endpoints (for Postman / curl testing) ───────────────────────────────
#   GET  http://localhost:8000/api/academics/semesters
#   GET  http://localhost:8000/api/academics/departments
#   GET  http://localhost:8000/api/academics/sections?semester=Spring+2026&subject=COMP
#   GET  http://localhost:8000/api/academics/sections?search=database&semester=Spring+2026
#   POST http://localhost:8000/api/academics/conflicts          body: { sections: [...] }
#   POST http://localhost:8000/api/academics/export/ics         body: { sections: [...], semester: "Spring 2026" }

version: "3.9"

services:
  backend:
    build:
      context: .
    container_name: campus-connect
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      PORT: "8000"
      NODE_ENV: development
    env_file:
      - .env
